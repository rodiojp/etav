import { DialogConfigFactory } from './dialog-config-factory';

describe('DialogConfigFactory', () => {
  it('should create an instance', () => {
    expect(new DialogConfigFactory()).toBeTruthy();
  });
});
