import { ProfileFormFactory } from './profile-form.factory';

describe('ProfileFormFactory', () => {
  it('should create an instance', () => {
    expect(new ProfileFormFactory()).toBeTruthy();
  });
});
